server:  
  port: 8080  

spring:  
  application:  
    name: config-server
  cloud:  
    config:  
      server:  
        git:  
          uri: https://github.com/drimkoe/config-repo  
          clone-on-start: true
  datasource:
    url: jdbc:mysql://localhost:3306/devdb
    username: devuser
    password: devpass